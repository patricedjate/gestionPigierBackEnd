package com.pigier.gestionStage.repositories;

import com.pigier.gestionStage.entities.Superviseurs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface superviseursRepository extends JpaRepository<Superviseurs, Integer> {
}
